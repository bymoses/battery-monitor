{
  "name": "bms-watchdog-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "build:rust": "./scripts/cargo-docker.sh build",
    "build:release": "./scripts/cargo-docker.sh build --release",
    "check:rust": "./scripts/cargo-docker.sh check",
    "test:rust": "./scripts/cargo-docker.sh test",
    "test:e2e": "bun run build:rust && bun test tests/e2e.test.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
